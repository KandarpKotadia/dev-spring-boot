# use wildcard '*' to expose all endpoints
# can also expose individual endpoints with a comma-delimited list
spring.application.name=mycoolapp
management.endpoints.web.exposure.include=*

# you can also exclude the endpoints with a comma-delimited list
# management.endpoints.web.exposure.exclude=health,info

management.info.env.enabled=true

info.app.name=My Super Cool App
info.app.description=A crazy app
info.app.version=1.0.0


# Defining our own properties
coach.name=Mickey Mouse
team.name=The Mouse Club

# changing the spring boot enbedded server port
server.port=7070

# setting the context path of the application
server.servlet.context-path=/sillyapp/